{
  "name": "wial-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "scaffold-chapter": "node --experimental-strip-types scripts/scaffold-chapter.ts",
    "clean-db": "node --experimental-strip-types scripts/clean-db.ts",
    "seed-demo": "node --experimental-strip-types scripts/seed-demo.ts"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.8.20"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=18"
  }
}
